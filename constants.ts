export const GEMINI_MODEL_NAME = 'gemini-3-flash-preview';
export const MAX_HINTS = 3;
export const CANVAS_SIZE = 400;
export const PEN_COLOR = '#FFFFFF';
export const PEN_WIDTH = 24;

export const MAX_RETRIES = 3; // Maximum number of times to retry an API call
export const INITIAL_RETRY_DELAY_MS = 1000; // Initial delay for API retries in milliseconds (1 second)
